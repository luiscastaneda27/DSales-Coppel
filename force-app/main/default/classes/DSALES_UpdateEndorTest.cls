@isTest
public class DSALES_UpdateEndorTest {
    @isTest
    static void endosoJob(){
        Test.startTest();
        DSALES_UpdateEndor actu = new DSALES_UpdateEndor();
        actu.execute(null);
       Test.stopTest(); 
    }

}